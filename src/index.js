import * as $ from 'jquery';

/* import json from '@assets/json.json'
import xml from '@assets/data.xml'
import csv from '@assets/data.csv' */
import '@/styles/styles.css'
import '@/styles/test_scss.scss'
import '@/styles/test_less.less'
import Post from '@models/post'
//const Post = require('./models/post')
//import '@/babel-test'
import WebpackLogo from '@assets/webpack.png'
//import '@/ts-test.ts'
//import '@/index-react.jsx'
import _ from 'lodash'
import {re} from "@babel/core/lib/vendor/import-meta-resolve";
// import '@/index_ts.ts'

/* const post = new Post("Post title foo", WebpackLogo)
console.log(post.toString())
document.querySelector(".post").innerHTML = JSON.stringify(json)

//console.log(post.putImg())
post.putImg()
console.log(post.outputJSON())
console.log('XML:', xml)
console.log('CSV:', csv) */

// let json_tag = $(".json").html(post.toString())

/* function func() {
	let t = 20;
} */

/*const unused_var = 20


let post;

$(".btn").addClass("test").on("click", (e) => {
	e.preventDefault()
	console.log("click")

	/!* if (json_tag.is(':hidden')) {
		json_tag.slideDown()
	} else {
		json_tag.slideUp()
	} *!/

	if ($("#post-output").children(".post").length === 0) {
		post = new Post("Post title foo", WebpackLogo)
		post.buildPost()
	} else {
		post.rebuildPost()
		post = undefined
	}
	
})*/



/*
import('lodash').then(() => {
	console.log("Lodash", _.random(0, 1000))
})*/
